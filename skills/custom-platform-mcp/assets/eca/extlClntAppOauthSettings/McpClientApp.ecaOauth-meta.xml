<?xml version="1.0" encoding="UTF-8"?>
<!--
  ECA OAuth scopes.

  THE SCOPE LITERAL IS  MCP  - NOT  mcp_api.   [org verified]
    mcp_api = what the CLIENT sends in the OAuth request.
    MCP     = what the METADATA expects here.
  A wrong value fails the deploy and helpfully lists all valid scopes.

  Verify after deploy:
    SELECT DeveloperName, OauthScopesMCP_API, OauthScopesREFRESH_TOKEN
    FROM ExtlClntAppOauthSettings          (tooling API - expect both true)
-->
<ExtlClntAppOauthSettings xmlns="http://soap.sforce.com/2006/04/metadata">
    <commaSeparatedOauthScopes>MCP, RefreshToken</commaSeparatedOauthScopes>
    <externalClientApplication>McpClientApp</externalClientApplication>
    <label>MCP Client App Scopes</label>
</ExtlClntAppOauthSettings>
